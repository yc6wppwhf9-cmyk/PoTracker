# What this service needs, in readable form. The versions actually installed
# are pinned in requirements.lock.txt, which is what deployment uses — see the
# note at the top of that file.
fastapi>=0.118
uvicorn[standard]>=0.34
python-multipart>=0.0.20
supabase>=2.15
openpyxl>=3.1.5
# Reads the PO number out of an attached PO document.
pypdf>=5.1
pydantic>=2.11
httpx>=0.28
anthropic>=0.49

python-dotenv>=1.0

# Dev / test
pytest>=8.0
